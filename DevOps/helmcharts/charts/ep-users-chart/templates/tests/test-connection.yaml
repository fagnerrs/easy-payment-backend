apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ep-users-chart.fullname" . }}-test-connection"
  labels:
    {{- include "ep-users-chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ep-users-chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
